# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
bin/
dist/

# Ignore binaries generated by go build
# (Common pattern is to ignore everything in cmd that isn't a .go file)
# But a safer bet for now is to just ignore common binary names or specific paths if known.
# Since we don't want cmd in releases, we already added it to .gitattributes.

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with Litmus
*.out

# Dependency directories (remove the comment below to include it)
# vendor/

# IDEs and Editors
.vscode/
.idea/
*.swp
*.swo

# OS files
.DS_Store
Thumbs.db

